---
name: "pulse-on"
description: "Enable CocoPulse ambient progress observation."
version: "2.0.1"
author: "CocoPlus"
tags:
  - cocoplus
  - cocopulse
---

Your objective is to implement `$pulse on`.

Create `.cocoplus/modes/pulse.on` and initialize `.cocoplus/pulse/config.json` when missing. CocoPulse observes progress and surfaces stale work; it must not approve gates or mutate lifecycle stage status.
